import { Button, Flex } from 'antd';

const SubmitButton = () => (
    <Flex gap="small" wrap >
      <Button size="large"
      type="primary">บันทึก</Button>
    </Flex>
  );
export default SubmitButton